'use babel';

import LanguageStormbreakerView from '../lib/language-stormbreaker-view';

describe('LanguageStormbreakerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
